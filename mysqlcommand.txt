MySQL Command Used To Create The Database

- CREATE TABLE IF NOT EXISTS requests (bookingNo VARCHAR(255) NOT NULL, customerName VARCHAR(255) NOT NULL, phone VARCHAR(12) NOT NULL, unitNumber VARCHAR(10), streetNumber INTEGER(2) NOT NULL, streetName VARCHAR(255) NOT NULL, suburb VARCHAR(255), destinationSuburb VARCHAR(255), pickupDate DATE NOT NULL, pickupTime TIME NOT NULL, assignmentStatus VARCHAR(255) NOT NULL)